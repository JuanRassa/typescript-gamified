import * as readlineSync from 'readline-sync'; // Don't worry about this
// We'll cover `import` in future levels.
const answer = readlineSync.question('Hello player, welcome!');
console.log(answer);
